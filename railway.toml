[build]
builder = "nixpacks"
buildCommand = "dotnet restore LogLens.sln && dotnet publish API/LogLens.API.csproj -c Release -o out"

[deploy]
startCommand = "dotnet out/LogLens.API.dll"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"