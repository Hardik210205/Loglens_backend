[build]
builder = "nixpacks"
buildCommand = "dotnet restore API/LogLens.API.csproj && dotnet publish API/LogLens.API.csproj -c Release -o out"

[deploy]
startCommand = "cd out && dotnet LogLens.API.dll"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"