[build]
builder = "nixpacks"
buildCommand = "dotnet publish API/LogLens.API.csproj -c Release -o out"

[deploy]
startCommand = "cd out && dotnet LogLens.API.dll"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"